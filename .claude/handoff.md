# Handoff — передача контекста между сессиями

---

## Последняя сессия: 2026-03-04

### Что сделали

**modules/analyzer.py**
- Переписан ANALYZE_PROMPT: теперь GPT-4o сначала читает напечатанный промпт с референса (дословно), и только если текст нечитаем — описывает изображение визуально
- Логика: STEP 1 (читай текст / описывай визуально) → STEP 2 (5 вариантов с разными позами)

**modules/generator.py**
- Исправлен критический баг SeeDream: модель image-only, нужен `modalities: ["image"]`
- Исправлен парсинг ответа: все модели возвращают картинку в `message.images[]`, не в `content`
- `_is_image_only_model()` — определяет тип модели по prefix (`bytedance-seed/` и др.)
- Drive структура: `week_{N}/{category}/seedream/ + seedream_pin/ + nanobana/ + nanobana_pin/`
- `_process_one` принимает `week: int`, сохраняет оба pin ID в DB
- Добавлено логирование промптов (`prompt_full`, `prompt_short`) в начале каждой генерации

**modules/bot.py**
- Убран блок `posting_status == running` на кнопке "Запустить постинг" — теперь можно добавлять пины в расписание пока постинг идёт
- Кнопки недели разделены: `◀ Неделя N ▶` + отдельная строка `▶ Генерация недели N`
- Добавлен `ReplyKeyboardMarkup` внизу чата (Pinterest / Telegram / ВКонтакте)
- Кнопка "🗑 Очистить всё" (`pin:clear`): удаляет Drive gens + очищает DB + сбрасывает статусы
- Обработчики текстовых кнопок reply-клавиатуры

**modules/scheduler.py**
- Каждая генерация → 2 записи в `pins_schedule` (SeeDream pin + NanaBana pin)
- Новые пины встают после последнего уже запланированного дня (не перезаписывают)
- Сохраняется оригинальный `posting_start_date` при добавлении новых недель

**database.py**
- Добавлена колонка `nb_pinterest_file_id` + автомиграция через `try/except ALTER TABLE`

**config.py**
- `MODEL_IMAGE_1 = "bytedance-seed/seedream-4.5"` (заменён GPT-5-image)

**CLAUDE.md + .claude/handoff.md + .claude/commands/**
- Создан паспорт проекта CLAUDE.md
- Команды `/morning` и `/endday` — перенесены в `C:/Users/Роман/.claude/commands/` (глобальные, работают из любой папки)

### Статус деплоя
Всё задеплоено на сервер (`syntx.service`). Последний коммит: `3b1d6b2`

---

## На завтра / следующая сессия

### Приоритет 1 — Тест полного цикла
1. "🗑 Очистить всё" в боте
2. Анализ референсов → проверить в логах что промпты читаются с изображений дословно
3. Генерация неделя 1 → проверить SeeDream: 5/5 ✓ и NanaBana: 5/5 ✓
4. Проверить папки на Drive: `week_1/категория/seedream_pin/` и `nanobana_pin/`
5. Запустить постинг → проверить расписание

### Приоритет 2 — SeeDream формат 2:3
- Проверить выдаёт ли SeeDream реально 2:3 или всё ещё 1:1
- `image_config: {"aspect_ratio": "2:3"}` передаётся, но неизвестно поддерживает ли его SeeDream

### Открытые вопросы
- Работает ли Make.com webhook с текущей структурой данных? (ещё не тестировали постинг до конца)
- SeeDream: поддерживает ли `image_config.aspect_ratio`?
- Качество SeeDream после исправления бага — нужно визуально оценить

### Следующий этап
После успешного теста Pinterest → Telegram-канал пайплайн

---

## Обещания / договорённости
- Не добавлять текстовые инструкции о формате/ориентации в промпты генерации — только через `image_config`
- При смене модели — обновлять названия папок на Drive и сообщения бота
- Референсы подбираются с напечатанным промптом → GPT читает его дословно (не игнорирует)
- SeeDream — image-only модель: `modalities: ["image"]`, ответ в `message.images[]`
