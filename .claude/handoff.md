# Handoff — передача контекста между сессиями

---

## Последняя сессия: 2026-03-03 → 2026-03-04

### Что сделали

**Генератор (modules/generator.py)**
- Заменили `openai/gpt-5-image` → `bytedance-seed/seedream-4.5` (GPT не давал 2:3 и дорогой)
- Убрали portrait-инструкцию из промпта (текстовые хаки — не наш путь)
- `image_config: {"aspect_ratio": "2:3"}` — единственный способ задать формат
- Папки на Drive теперь `week_{N}/{category}/seedream/` вместо `{date}_{category}/gpt/`
- `_process_one` принимает `week: int`, сохраняет `nb_pinterest_file_id` в DB

**База данных (database.py)**
- Добавлена колонка `nb_pinterest_file_id` в таблицу `generations`
- Автомиграция через `try/except ALTER TABLE`

**Планировщик (modules/scheduler.py)**
- Исправлен критический баг: NanaBana пины не попадали в постинг
- Теперь каждая генерация → 2 записи в `pins_schedule` (SeeDream pin + NanaBana pin)

**Бот (modules/bot.py)**
- Добавлен `ReplyKeyboardMarkup` внизу чата: Pinterest / Telegram / ВКонтакте
- Кнопка "🗑 Очистить всё" (pin:clear): удаляет Drive gens + очищает DB + сбрасывает статусы
- Кнопки недели разделены: `◀ Неделя N ▶` + отдельная строка `▶ Генерация недели N`
- Обработчики текстовых кнопок из reply-клавиатуры

### Статус деплоя
- Все изменения запушены в GitHub (commit `5988c16`)
- **НЕ задеплоено** на сервер — Таймвеб был недоступен
- Команда для деплоя: `cd ~/Autopasting-Syntx && git pull && sudo systemctl restart syntx`

---

## На завтра / следующая сессия

### Приоритет 1 — задеплоить
```bash
cd ~/Autopasting-Syntx && git pull && sudo systemctl restart syntx
```

### Приоритет 2 — тест полного цикла
1. "🗑 Очистить всё" в боте
2. Анализ референсов (проверить что создаются 5 вариантов промпта)
3. Генерация неделя 1 (проверить: Drive `week_1/` → `seedream_pin/` + `nanobana_pin/`)
4. Проверить формат 2:3 у обеих моделей
5. Запустить постинг (проверить что в расписание попали ОБА пина с каждой генерации)

### Открытые вопросы
- SeeDream 4.5 — соблюдает ли `image_config.aspect_ratio: "2:3"`? Проверить после первой генерации
- Качество SeeDream vs NanaBana — визуально сравнить после теста
- Make.com webhook — работает ли с текущей структурой данных?

### Следующий этап (после успешного теста Pinterest)
- Telegram-канал пайплайн
- ВКонтакте пайплайн

---

## Обещания / договорённости
- Не добавлять текстовые инструкции о формате/ориентации в промпты генерации — только через `image_config`
- При смене модели — всегда обновлять названия папок и сообщения бота
